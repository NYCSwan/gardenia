import React, { Component } from 'react';
import { database } from './firebase';
import PropTypes from 'prop-types';

function Note(props) {
  return (
    <div className="note">
      <p>note</p>
    </div>
  );
}
export default Note;
